<Window x:Class="FormulaExplorer.UI.ExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="520" Height="360"
        MinWidth="320" MinHeight="200"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        UseLayoutRounding="True">

    <!--
        M1: raw formula text and nothing else. The structural TreeView and the
        span-highlighted formula box arrive in M2. Esc closes (see code-behind).
    -->
    <DockPanel Margin="10">

        <TextBlock DockPanel.Dock="Top"
                   Text="{Binding CellAddress}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <Border BorderBrush="#FFC8C8C8" BorderThickness="1" Background="#FFFAFAFA">
            <TextBox Text="{Binding RawFormula, Mode=OneWay}"
                     IsReadOnly="True"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Consolas, Cascadia Mono, Courier New"
                     FontSize="13"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Padding="8" />
        </Border>

    </DockPanel>
</Window>
